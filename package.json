{
  "name": "input-server",
  "version": "0.0.1",
  "description": "remote input sending server",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^4.5.0"
  },
  "scripts": {
    "start": "node index.js"
  }
}
